# 🚀 快速安装指南

## 当前状态
- ✅ Node.js v22.2.0 已安装
- ✅ npm v10.7.0 已安装  
- ❌ 项目依赖未安装（需要安装）

## 🎯 推荐方法（按优先级）

### 方法1: 使用npm安装（最简单）

在终端中执行以下命令：

```bash
cd "/Users/yushifu24/Documents/Side Projects/Repos/2526WinterChallenge"
npm install
```

如果遇到权限错误，尝试：
```bash
npm install --legacy-peer-deps
```

---

### 方法2: 使用安装脚本

我已经为你创建了一个安装脚本：

```bash
cd "/Users/yushifu24/Documents/Side Projects/Repos/2526WinterChallenge"
bash install.sh
```

这个脚本会自动尝试多种安装方法。

---

### 方法3: 使用yarn（如果npm有问题）

```bash
# 1. 安装yarn
npm install -g yarn

# 2. 安装项目依赖
cd "/Users/yushifu24/Documents/Side Projects/Repos/2526WinterChallenge"
yarn install
```

---

## ✅ 验证安装成功

安装完成后，运行以下命令验证：

```bash
# 检查node_modules是否存在
ls node_modules/react

# 应该能看到react目录，如果看到说明安装成功！

# 启动开发服务器
npm run dev
```

如果看到类似以下输出，说明成功了：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## 🐛 如果遇到问题

### 问题1: npm权限错误
```bash
# 尝试使用sudo（macOS/Linux）
sudo npm install

# 或清理缓存后重试
npm cache clean --force
npm install
```

### 问题2: 网络连接慢（中国用户）
```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

### 问题3: 版本冲突
```bash
# 使用legacy peer deps
npm install --legacy-peer-deps
```

---

## 📖 详细文档

- **INSTALL_GUIDE.md** - 完整的安装指南和故障排除
- **SETUP.md** - 项目设置和配置说明

---

## 🎉 安装完成后

1. **启动开发服务器**:
   ```bash
   npm run dev
   ```

2. **打开浏览器**:
   访问 http://localhost:5173

3. **开始开发**:
   现在应该没有编译错误了！

---

## 💡 提示

- 如果npm一直有问题，强烈推荐使用yarn
- 安装过程可能需要几分钟，请耐心等待
- 如果遇到任何问题，查看 `INSTALL_GUIDE.md` 获取详细帮助


